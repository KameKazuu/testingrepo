name: Run Tests

on:
  workflow_run:
    workflows:
      - Bundle and Deploy
    types:
      - completed
  schedule:
    - cron: "0 * * * *"
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Branch
        uses: actions/checkout@v6
      - name: Setup Node.js Environment
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - name: Install Dependencies
        run: npm ci
      - name: Run Tests
        # These are live integration tests against the real sites. CI runners
        # have no Cloudflare cf_clearance, so Cloudflare-gated sources (Mangago,
        # Mangapill, WeebCentral, Webtoon, ...) can't pass here and an unhandled
        # CloudflareError can abort the whole run. Keep the results visible but
        # don't let them block PRs — conformance + CodeQL are the real gates.
        continue-on-error: true
        run: npm test
      - name: Upload Test Results
        if: ${{ github.event_name != 'pull_request' }}
        uses: actions/upload-artifact@v7
        with:
          name: tests.json
          path: bundles/tests.json
